export CLUSTER_VERSION=local
export COMMIT_SHA="dummy-commit-sha"
export BRANCH="dummy-branch"
export PRISMA_CONFIG_PATH=$(pwd)/prisma.yml
export RABBITMQ_URI=amqp://localhost
export SERVER_ROOT=$(pwd)